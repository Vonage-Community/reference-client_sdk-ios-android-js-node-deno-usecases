{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "dotenv -- turbo run dev",
    "dev:contact-center": "dotenv -- turbo run dev --filter='./contact-center/*' --filter=setup-project-webhooks",
    "dev:multichannel-app": "dotenv -- turbo run dev --filter='./multichannel-app/*' --filter=setup-project-webhooks",
    "db:start": "turbo run db:start",
    "db:stop": "turbo run db:stop",
    "db:reset": "turbo run db:reset",
    "lint": "turbo run lint",
    "deploy": "turbo run deploy"
  },
  "devDependencies": {
    "dotenv-cli": "^7.2.1",
    "eslint": "^8.44.0",
    "eslint-config-custom": "*",
    "prettier": "^2.5.1",
    "tailwindcss": "^3.3.2",
    "turbo": "^1.10.1"
  },
  "name": "web",
  "packageManager": "npm@8.19.3",
  "workspaces": [
    "contact-center/*",
    "multichannel-app/*",
    "packages/*",
    "scripts/*"
  ],
  "dependencies": {
    "@vonage/client-sdk": "1.2.1",
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.23"
  }
}
